import { MouseHover } from './mouse-hover';

describe('MouseHover', () => {
  it('should create an instance', () => {
    const directive = new MouseHover();
    expect(directive).toBeTruthy();
  });
});
